# Phase 5: High Fidelity 2.5D Overhaul Concept

Here is the fully realized concept visualization for our target styling! Based on the requested layout preservation, we will trace the core structure from the 2D grid matrix into this isometric perspective logic frame.

![Isometric Hospital Simulation Overhaul](file:///C:/Users/mrawan/.gemini/antigravity/brain/ac98022f-19dd-4200-ab23-7098ed283d97/isometric_hospital_concept_1776819016080.png)

### Core Architectural Targets

1. **Isometric Projection Refactor**: Shifting the primary map overlay out of direct native 2D CSS-grid `X-Y` positioning and plotting the array through a true `.isometric-bg` skewed matrix calculation `(rotateX, rotateZ)`.
2. **Prop & Environment Texturing**: Swapping the base Tailwind hex colors for imported high-fidelity sprite blocks. We will map distinct sprites aligned identically to the original Matrix (Green floors for Wards, MRI props mapping directly onto cell bounds).
3. **Immersive Overlay Interactivity**: Anchoring the NextJS standard DOM elements strictly around the perimeter (Sleek lower-edge stats, right-aligned controls) to maintain that hyper-clean Theme Hospital simulation window aesthetic!
